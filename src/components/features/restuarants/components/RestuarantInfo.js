import { StyleSheet, Text, View } from "react-native";
import React from "react";

const RestuarantInfo = () => {
  return (
    <View>
      <Text>RestuarantInfo</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default RestuarantInfo;
